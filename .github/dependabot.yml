version: 2

# Workflow actions are pinned to commit SHAs, so updates cannot arrive by
# tag movement and must come through a pull request instead.
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
